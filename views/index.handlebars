<h1>Delicious Hamburgers!</h1>

<h2>Burgers that are not eaten!</h2>

<ul>
  {{#each burgers}}
    {{#unless burger_eaten}}
      {{> burgers/burger-block eaten=false}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that are eaten!</h2>

<ul>
  {{#each burgers}}
    {{#if burger_eaten}}
      {{> burgers/burger-block eaten=true}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="burg">Burger Name:</label>
    <input type="text" id="burg" name="name">
  </div>

  <div class="form-group">
    <label for="is-eaten">Has it been eaten?</label><br>
    <input type="radio" name="is-eaten" value="1" checked> Eaten!<br>
    <input type="radio" name="is-eaten" value="0"> Not Eaten.
  </div>

  <button type="submit">Add burger.</button>
</form>
